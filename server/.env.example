# Für lokale Entwicklung (lokales MongoDB):
MONGODB_URI=mongodb://localhost:27017/icecream-ratings

# Für Produktion (MongoDB Atlas - nach der Registrierung ersetzen):
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/icecream-ratings?retryWrites=true&w=majority

PORT=4000